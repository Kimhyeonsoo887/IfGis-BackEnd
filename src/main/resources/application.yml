spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/IFgisDat?serverTimezone=UTC&characterEncoding=UTF-8
    username: ifgis
    password: 158whstj

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: update
  servlet:
    multipart:
      maxFileSize: 10MB
      maxRequestSize: 20MB
logging:
  level:
    org.hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
uploadDir: /Users/hyunsookim/ifgisweb/src/uploadImg/